# Checkout the docs => https://www.gitpod.io/docs/config-gitpod-file
image: rust:latest

tasks:
  - init: |
      curl -sL install.mob.sh | sh
      cargo build
    
vscode:
  extensions:
    - matklad.rust-analyzer

